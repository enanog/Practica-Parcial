#include <stdio.h>

/*
A) Ocupa exactamente lo mismo porque lo que almacena es la direccion
de memoria, esto depende unicamente de la plataforma
B) Funciona correctamente lo que hace es lo mismo que str[0] + 3
Imprime 'S'.
C) Imprime 'o'
d) Devuelve 2028
*/

int main(void)
{
    return 0;
}

